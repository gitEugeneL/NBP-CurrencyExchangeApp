// android emulator (android studio)
const androidEmulatorHost = '10.0.2.2';
// localhost web port
const localPort = '5044';
// docker localhost web port
const dockerPort = '8000';

export const PREFIX = `http://${androidEmulatorHost}:${dockerPort}/api`;
