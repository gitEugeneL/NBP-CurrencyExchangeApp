.card {
  display: flex;
  width: 100%;
  height: 20px;
  background-color: var(--red-color);
  color: var(--white-color);
  justify-content: center;
  align-items: center;
  font-size: var(--font-size12);
}
